// Placeholder for future interactive features
console.log("Cyber Awareness project loaded.");

// Example: log which page is active
if(document.body.contains(document.querySelector('h2'))) {
    console.log("Page loaded: " + document.querySelector('h2').innerText);
}
